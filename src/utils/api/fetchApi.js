/* export const fetchApi = async (route, bodyContent, method = 'GET') => {
  const body =
    method !== 'GET' && bodyContent ? JSON.stringify(bodyContent) : null;
  return await fetch(`${process.env.REACT_APP_API_URL}${route}`, {
    method,
    body
  }).then(response => response.json());
}; */



  

